#
# docker-compose.prod.dokploy.yml
#
# Compose unificado para despliegue en Dokploy (Traefik como reverse proxy).
# Integra todos los servicios de services/postgres.yaml, services/redis.yaml y
# services/powersync.yaml en un solo archivo sin directivas `include:`.
#
# ANTES DE DESPLEGAR, actualiza las variables secretas en config/prod.env:
#   - SECRET_KEY           (genera con: python -c "import secrets; print(secrets.token_urlsafe(50))")
#   - JWT_PUBLIC_KEY / JWT_PRIVATE_KEY  (genera con: docker compose exec web ./manage.py generate-jwt-keys)
#   - POSTGRES_PASSWORD    (cambia el valor por defecto 'wger')
#   - PS_DATABASE_URI / PS_STORAGE_PG_URI  (actualiza con el nuevo password de postgres)
#
# web / celery_worker / celery_beat se construyen desde el código fuente en
# ./server (fork de wger-project/wger incorporado vía git subtree), usando el
# Dockerfile de producción oficial de wger. Para actualizar wger:
#   git subtree pull --prefix=server upstream master --squash
#

services:

  # ---------------------------------------------------------------------------
  # Aplicación principal Django/Gunicorn
  # ---------------------------------------------------------------------------
  web:
    build:
      context: ./server
      dockerfile: extras/docker/production/Dockerfile
      args:
        BUILD_COMMIT: ${BUILD_COMMIT:-local}
        BUILD_DATE: ${BUILD_DATE:-}
    logging:
      driver: json-file
      options:
        max-size: 10m
        max-file: 3
    depends_on:
      db:
        condition: service_healthy
      cache:
        condition: service_healthy
    env_file:
      - ./config/prod.env
    volumes:
      - static:/home/wger/static
      - media:/home/wger/media
    expose:
      - 8000
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:8000"]
      interval: 10s
      timeout: 5s
      start_period: 300s
      retries: 5
    deploy:
      resources:
        limits:
          memory: 768M
          cpus: "1.0"
        reservations:
          memory: 256M
    restart: unless-stopped
    networks:
      - wger_network

  # ---------------------------------------------------------------------------
  # Nginx: sirve archivos estáticos/media y hace proxy a Gunicorn y PowerSync.
  # Traefik (Dokploy) enruta el tráfico externo hacia este contenedor en el
  # puerto 80. El SSL y los certificados los gestiona Traefik.
  # ---------------------------------------------------------------------------
  nginx:
    image: docker.io/nginx:1.30.3-alpine
    logging:
      driver: json-file
      options:
        max-size: 10m
        max-file: 3
    depends_on:
      - web
      - powersync
    volumes:
      - ./config/nginx.conf:/etc/nginx/conf.d/default.conf
      - static:/wger/static:ro
      - media:/wger/media:ro
    expose:
      - 80
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://127.0.0.1/"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 30s
    restart: unless-stopped
    networks:
      - wger_network
      - dokploy-network
    labels:
      - "traefik.enable=true"

      # -----------------------------------------------------------------------
      # Dominio principal: rippedandrenew.com + www.rippedandrenew.com
      # -----------------------------------------------------------------------
      # HTTP
      - "traefik.http.routers.rippedandrenew-wger-1-web.rule=Host(`rippedandrenew.com`) || Host(`www.rippedandrenew.com`)"
      - "traefik.http.routers.rippedandrenew-wger-1-web.entrypoints=web"
      - "traefik.http.routers.rippedandrenew-wger-1-web.service=rippedandrenew-wger-1-web"
      - "traefik.http.services.rippedandrenew-wger-1-web.loadbalancer.server.port=80"
      # HTTPS
      - "traefik.http.routers.rippedandrenew-wger-1-websecure.rule=Host(`rippedandrenew.com`) || Host(`www.rippedandrenew.com`)"
      - "traefik.http.routers.rippedandrenew-wger-1-websecure.entrypoints=websecure"
      - "traefik.http.routers.rippedandrenew-wger-1-websecure.service=rippedandrenew-wger-1-websecure"
      - "traefik.http.routers.rippedandrenew-wger-1-websecure.tls.certresolver=letsencrypt"
      - "traefik.http.services.rippedandrenew-wger-1-websecure.loadbalancer.server.port=80"

      # -----------------------------------------------------------------------
      # Middleware: redirect 301 de dominios alias → rippedandrenew.com
      # -----------------------------------------------------------------------
      - "traefik.http.middlewares.rnr-alias-redirect.redirectregex.regex=^https?://(www\\.)?(rippedandrenew\\.net|rippedandrenew\\.info|rippedandrenew\\.org)(.*)"
      - "traefik.http.middlewares.rnr-alias-redirect.redirectregex.replacement=https://rippedandrenew.com$${3}"
      - "traefik.http.middlewares.rnr-alias-redirect.redirectregex.permanent=true"

      # -----------------------------------------------------------------------
      # Dominios alias HTTP → redirect a rippedandrenew.com
      # -----------------------------------------------------------------------
      - "traefik.http.routers.rnr-alias-web.rule=Host(`rippedandrenew.net`) || Host(`www.rippedandrenew.net`) || Host(`rippedandrenew.info`) || Host(`www.rippedandrenew.info`) || Host(`rippedandrenew.org`) || Host(`www.rippedandrenew.org`)"
      - "traefik.http.routers.rnr-alias-web.entrypoints=web"
      - "traefik.http.routers.rnr-alias-web.middlewares=rnr-alias-redirect"
      - "traefik.http.routers.rnr-alias-web.service=rippedandrenew-wger-1-web"

      # -----------------------------------------------------------------------
      # Dominios alias HTTPS → redirect a rippedandrenew.com (con SSL propio)
      # -----------------------------------------------------------------------
      - "traefik.http.routers.rnr-alias-websecure.rule=Host(`rippedandrenew.net`) || Host(`www.rippedandrenew.net`) || Host(`rippedandrenew.info`) || Host(`www.rippedandrenew.info`) || Host(`rippedandrenew.org`) || Host(`www.rippedandrenew.org`)"
      - "traefik.http.routers.rnr-alias-websecure.entrypoints=websecure"
      - "traefik.http.routers.rnr-alias-websecure.middlewares=rnr-alias-redirect"
      - "traefik.http.routers.rnr-alias-websecure.tls.certresolver=letsencrypt"
      - "traefik.http.routers.rnr-alias-websecure.service=rippedandrenew-wger-1-web"
    deploy:
      resources:
        limits:
          memory: 64M
          cpus: "0.25"
        reservations:
          memory: 32M

  # ---------------------------------------------------------------------------
  # PostgreSQL 15
  # ---------------------------------------------------------------------------
  db:
    image: docker.io/postgres:15.18-alpine
    shm_size: 256mb
    logging:
      driver: json-file
      options:
        max-size: 10m
        max-file: 5
    env_file:
      - ./config/prod.env
    environment:
      - TZ=America/New_York
    volumes:
      - postgres-data:/var/lib/postgresql/data/
    expose:
      - 5432
    command: [
      "postgres",
      "-c", "wal_level=logical",
      "-c", "shared_buffers=256MB",
      "-c", "effective_cache_size=768MB",
      "-c", "work_mem=8MB",
      "-c", "random_page_cost=1.1",
      "-c", "max_connections=30"
    ]
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U \"$$POSTGRES_USER\""]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 30s
    deploy:
      resources:
        limits:
          memory: 512M
          cpus: "1.0"
        reservations:
          memory: 256M
    restart: unless-stopped
    networks:
      - wger_network

  # ---------------------------------------------------------------------------
  # Redis (caché y broker de Celery)
  # ---------------------------------------------------------------------------
  cache:
    image: docker.io/redis:8.8.0-alpine
    logging:
      driver: json-file
      options:
        max-size: 10m
        max-file: 3
    expose:
      - 6379
    volumes:
      - ./config/redis.conf:/usr/local/etc/redis/redis.conf
      - redis-data:/data
    command: ["redis-server", "/usr/local/etc/redis/redis.conf"]
    healthcheck:
      test: redis-cli ping
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 30s
    deploy:
      resources:
        limits:
          memory: 128M
          cpus: "0.5"
        reservations:
          memory: 64M
    restart: unless-stopped
    networks:
      - wger_network

  # ---------------------------------------------------------------------------
  # PowerSync (sincronización offline para la app móvil)
  # ---------------------------------------------------------------------------
  powersync:
    image: docker.io/journeyapps/powersync-service:1.22.0
    logging:
      driver: json-file
      options:
        max-size: 10m
        max-file: 3
    command: ["start", "-r", "unified"]
    depends_on:
      db:
        condition: service_healthy
      web:
        condition: service_healthy
    volumes:
      - ./services/config-powersync:/config:ro
    env_file:
      - ./config/prod.env
    environment:
      POWERSYNC_CONFIG_PATH: /config/powersync.yaml
      PS_JWKS_URL: http://web:8000/api/v2/powersync-keys
    expose:
      - 8080
      - 9090
    deploy:
      resources:
        limits:
          memory: 512M
          cpus: "0.5"
        reservations:
          memory: 128M
    restart: unless-stopped
    networks:
      - wger_network

  # ---------------------------------------------------------------------------
  # Celery Worker (tareas asíncronas: sync de ejercicios, ingredientes, etc.)
  # ---------------------------------------------------------------------------
  celery_worker:
    build:
      context: ./server
      dockerfile: extras/docker/production/Dockerfile
    logging:
      driver: json-file
      options:
        max-size: 10m
        max-file: 3
    command: /start-worker
    env_file:
      - ./config/prod.env
    volumes:
      - media:/home/wger/media
    depends_on:
      web:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "celery", "-A", "wger", "inspect", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 30s
    deploy:
      resources:
        limits:
          memory: 512M
          cpus: "1.0"
        reservations:
          memory: 128M
    restart: unless-stopped
    networks:
      - wger_network

  # ---------------------------------------------------------------------------
  # Celery Beat (scheduler de tareas periódicas)
  # ---------------------------------------------------------------------------
  celery_beat:
    build:
      context: ./server
      dockerfile: extras/docker/production/Dockerfile
    logging:
      driver: json-file
      options:
        max-size: 10m
        max-file: 3
    command: /start-beat
    volumes:
      - celery-beat:/home/wger/beat/
    env_file:
      - ./config/prod.env
    depends_on:
      celery_worker:
        condition: service_healthy
    deploy:
      resources:
        limits:
          memory: 256M
          cpus: "0.25"
        reservations:
          memory: 32M
    restart: unless-stopped
    networks:
      - wger_network

# -----------------------------------------------------------------------------
# Volúmenes
# -----------------------------------------------------------------------------
volumes:
  postgres-data:
  celery-beat:
  redis-data:
  media:
  static:

# -----------------------------------------------------------------------------
# Redes
# -----------------------------------------------------------------------------
networks:
  wger_network:
    name: rippedandrenew-wger
    external: true
  dokploy-network:
    name: dokploy-network
    external: true
